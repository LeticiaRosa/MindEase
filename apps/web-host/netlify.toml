[build]
  command    = "pnpm install --frozen-lockfile && pnpm turbo run build --filter=web-host"
  publish    = "apps/web-host/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
